# Copy to .env.local and fill in from `supabase status` (local) or your project
# settings (cloud). Vite only exposes vars prefixed with VITE_.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=your-anon-key-here
